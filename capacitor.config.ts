import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mindsound',
  appName: 'MindSound',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;